# H4 · login-gate v0.5

**给**：接手实现的研发
**原型**：`features/login-gate/index.html`（本单不复述内容，原型本体就是任务正文）

---

## 这版是什么

**评审体验升级版：登录页设计与实现范围与 v0.4 完全相同，一个区块都没变。** 变的是你评审时用的界面（Baton 0.6.2）：

- **左上角新增「原型 | 台账」互切 tab**——两页互跳，不用记路径
- **右侧评论列表的卡片可以点了**——点任意一条：页面滚到对应区块并高亮，同时弹出该线程的完整内容（提问 + 全部回复 + 状态）。此前列表只显示提问前 90 字、回复看不到，是缺陷，已修
- **回帖支持两段式**——后续的代拟回帖正文只有两三行提炼，标「AI 代拟」，点「原话与完整理由 ↗」看完整推导。v0.4 里那三条长回帖是旧格式，保留原样

这些能力来自 Baton 工具层（`git pull` 后所有原型自动获得），不是本功能的定制。

## 要实现什么

**与 H3 完全一致，无新增无删减。** 已按 H3 开工的，继续，不受本版影响；还没开工的，以 H3 的十条清单为准（载体清单、三条演示替身、五条约束 D1/D2/D3/D5/D6 同样原样有效——安全相关的两条替身别漏：管理口令必须服务端校验、会话必须服务端签发）。

## 开放问题

同 H3，无变化。

---

打开原型：`node .baton/scripts/open.mjs login-gate`（装过全局命令则 `baton open login-gate`）；评论直接点页面区块，@AI 可查台账
